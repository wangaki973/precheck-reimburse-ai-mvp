# AI 报销预检助手
## 产品需求文档 · MVP v1.3

| 项目 | 内容 |
|------|------|
| 文档版本 | v1.3 |
| 文档状态 | MVP 已上线，持续迭代 |
| 更新日期 | 2026-04-28 |
| 作者 | Aki |
| 仓库地址 | https://github.com/wangaki973/precheck-reimburse-ai-mvp |

---

## 一、项目背景

### 1.1 问题描述

企业员工在提交报销前，普遍存在"不清楚规则、不确定能不能报"的困境。报销规则条款多、描述抽象，员工只能凭经验提交，导致财务驳回率居高不下。被驳回后员工需要修改重提，财务需要重复审核，整体流程效率低下。

核心痛点集中在两个角色：

- **员工**：不知道这张发票能不能报、报多少、需要补什么材料，提交后才被退回，来回周期长
- **财务**：大量标准项重复审核消耗人力，真正需要人工判断的异常项反而处理不过来

### 1.2 产品定位

AI 报销预检助手是一个**报销前置风控工具**。

它做且只做一件事：**员工在提交报销前，上传发票，系统通过 OCR 识别发票信息、RAG 检索企业报销规则、LLM 生成合规判断与说明，实时告知员工这笔费用是否合规，以及原因和建议。**

产品介入的时间点是：员工按下"提交"按钮之前的那一刻——这是系统能干预的最早位置，也是成本最低的干预点。

### 1.3 业务目标

| 目标指标 | 当前基线 | 目标值 |
|----------|----------|--------|
| 报销驳回率 | 25% | ≤ 10% |
| 员工提报等待时长 | 2–3 天 | 即时反馈（< 10 秒）|
| 财务标准项审核耗时 | 5 分钟/单 | 2 分钟/单 |

### 1.4 价值主张

- **对员工**：提交前获得实时合规反馈，减少因规则不熟悉导致的返工
- **对财务**：减少标准项重复审核，聚焦异常项处理
- **对企业**：降低合规风险，积累报销数据为后续费控策略优化提供依据

---

## 二、目标用户

### 2.1 核心角色

本版本只服务一个核心角色：**普通员工（报销发起人）**。

| 角色 | 使用场景 | 核心诉求 |
|------|----------|----------|
| 普通员工 | 整理好发票、准备提交报销前 | 快速知道"这张票能不能报"，减少被驳回 |

### 2.2 典型使用场景

**场景：报销前自检**

- 触发条件：员工整理好发票，准备提交报销申请
- 用户行为：上传发票图片或拍照
- 期望结果：5 秒内获得"通过 / 预警 / 拦截"判断，并附带原因说明和操作建议

---

## 三、核心流程

### 3.1 主流程（端到端）

```
员工进入系统
    ↓
上传发票图片（拍照 / 相册选择）
    ↓
【AI 节点 1】OCR 识别
  - 提取发票结构化字段
  - 返回识别结果供用户确认
    ↓
用户确认字段准确性
  → 准确：直接发起预检
  → 不准确：手动修改后发起预检
    ↓
【AI 节点 2】RAG 检索 + LLM 合规判断
  - 根据字段内容检索企业规则知识库
  - 将发票字段 + 匹配规则输入 LLM
  - LLM 输出合规状态 + 原因 + 建议
    ↓
展示预检结论（通过 / 预警 / 拦截）
    ↓
用户决策：采纳建议 / 修改重试 / 忽略继续
```

### 3.2 状态流转

```
空闲 → 识别中 → 确认中 → 判断中 → 通过 / 预警 / 拦截 → 结束或重新判断
```

### 3.3 异常流程

| 场景 | 触发条件 | 系统处理 | 用户提示 |
|------|----------|----------|----------|
| 发票识别失败 | OCR 无法识别图片内容 | 返回失败状态，不进入判断环节 | "无法识别发票，请检查图片清晰度后重试" |
| 字段置信度低 | 关键字段识别置信度低于阈值 | 标注"需人工确认"，不自动填充 | 对应字段高亮提示，引导用户手动核对 |
| 规则未覆盖 | 发票类型或费用类型不在规则库中 | 返回"待人工审核"状态 | "暂无法自动判断，请联系财务确认" |
| 网络异常 | 请求超时或网络中断 | 本地缓存已填内容，提示重试 | "网络异常，已保留内容，请稍后重试" |
| 字段修改后重判 | 用户修改了识别字段 | 触发二次合规判断 | 展示更新后的预检结论 |

---

## 四、AI 技术节点详细说明

本产品的核心 AI 链路由三个节点串联组成：**OCR 识别 → RAG 检索 → LLM 生成**。以下逐节点说明输入、处理逻辑、输出格式和边界约束。

---

### 4.1 节点一：OCR 发票识别

**作用**：将发票图片转化为结构化字段，供后续合规判断使用。

**输入**：发票图片文件（JPG / PNG / PDF）

**处理方式**：调用多模态视觉模型或专用 OCR 服务识别票面文字，提取关键字段。

**Prompt 设计要点**：
```
角色：你是一个发票信息提取助手。
任务：从用户上传的发票图片中，提取以下字段，以 JSON 格式返回。
字段列表：发票类型、发票号码、开票日期、开票单位、金额（不含税）、税额、价税合计、费用类型。
约束：
  1. 无法识别的字段返回 null，不得猜测填写
  2. 每个字段附带置信度分数（0-1）
  3. 只返回 JSON，不输出任何解释文字
```

**输出格式**：
```json
{
  "invoice_type": "增值税普通发票",
  "invoice_number": "12345678",
  "invoice_date": "2026-04-20",
  "merchant_name": "北京海底捞餐饮有限公司",
  "amount": 315.04,
  "tax_amount": 52.96,
  "total_amount": 368.00,
  "expense_type": "餐饮费",
  "confidence": {
    "invoice_number": 0.98,
    "amount": 0.99,
    "merchant_name": 0.95
  }
}
```

**边界约束**：
- 置信度 < 0.85 的字段，前端标注"请人工确认"
- 发票类型不在支持范围内（增值税普票/专票/电子票），返回"暂不支持该类型发票"
- MVP 阶段使用 mock 数据模拟，接口路径：`POST /api/ocr`

---

### 4.2 节点二：RAG 检索企业规则知识库

**作用**：根据发票字段（费用类型、金额、日期等）从企业规则知识库中检索最相关的报销规则条款，为 LLM 判断提供依据。

**输入**：OCR 提取的结构化字段（费用类型为主要检索维度）

**处理逻辑**：
1. 将费用类型、金额区间作为检索条件
2. 从规则知识库中匹配相关规则条款（向量检索 + 关键词过滤）
3. 返回 Top-3 相关规则，传入下一节点

**当前规则库（MVP 内置 5 条）**：

| 规则 ID | 费用类型 | 规则内容 | 违规类型 |
|---------|----------|----------|----------|
| R-01 | 餐饮费 | 单张发票金额 ≤ ¥200 | 硬性违规（拦截）|
| R-02 | 住宿费 | 单晚 ≤ ¥800（北京/上海） | 软性违规（预警）|
| R-03 | 交通费 | 单次 ≤ ¥1,500（高铁/飞机）| 软性违规（预警）|
| R-04 | 全类型 | 发票日期须在申报出差区间内 | 硬性违规（拦截）|
| R-05 | 全类型 | 发票抬头须与公司名称一致 | 硬性违规（拦截）|

**边界约束**：
- 费用类型不在规则库覆盖范围内 → 返回"规则未覆盖"，转人工审核
- MVP 阶段规则库为硬编码，后续版本支持管理员配置

---

### 4.3 节点三：LLM 合规判断与回答生成

**作用**：将发票字段和检索到的规则一起输入 LLM，由 LLM 进行合规判断，并生成面向员工的自然语言解释和操作建议。

**输入**：
- 发票结构化字段（来自节点一）
- 匹配规则列表（来自节点二）

**Prompt 设计要点**：
```
角色：你是一个企业报销合规审核助手，只能基于提供的规则进行判断，不得自行推断规则。

规则列表：
[此处动态插入 RAG 检索到的规则条款]

发票信息：
[此处动态插入 OCR 识别的字段 JSON]

任务：判断该发票是否符合报销规范，返回以下 JSON 格式结果：
{
  "status": "pass | warn | block",
  "triggered_rule": "规则ID或null",
  "reason": "一句话说明判断原因",
  "suggestion": "具体操作建议"
}

约束：
  1. 只能基于提供的规则判断，不得凭常识推断
  2. 规则库未覆盖的情况，status 返回 "pending"，reason 写"规则未覆盖，请联系财务"
  3. 同时触发多条规则时，优先展示最高级别违规（block > warn）
  4. reason 和 suggestion 面向员工，语气友好，避免指责
  5. 只返回 JSON，不输出任何额外文字
```

**输出格式**：
```json
{
  "status": "block",
  "triggered_rule": "R-01",
  "reason": "您的餐饮发票金额为 ¥368，超出公司单张餐饮报销限额 ¥200。",
  "suggestion": "建议将该餐饮费用拆分为多张发票分次报销，或联系财务申请特殊审批。"
}
```

**前端展示对应**：

| status | 展示样式 | 操作引导 |
|--------|----------|----------|
| pass | ✅ 绿色 · 合规通过 | "提交报销" |
| warn | ⚠️ 橙色 · 存在风险 | "填写说明后提交" |
| block | ❌ 红色 · 不符合规范 | "查看修改建议" |
| pending | 🔍 灰色 · 待人工审核 | "联系财务确认" |

**接口路径**：`POST /api/precheck`

---

## 五、功能范围

### 5.1 MVP 已实现功能（v1.3 当前版本）

| 功能模块 | 功能点 | 状态 |
|----------|--------|------|
| 发票识别 | 拍照/相册上传 | ✅ 已实现（mock）|
| 发票识别 | OCR 字段提取展示 | ✅ 已实现（mock）|
| 发票识别 | 字段手动修正 | ✅ 已实现 |
| 合规判断 | 规则匹配判断 | ✅ 已实现（mock）|
| 合规判断 | 三种状态展示（通过/预警/拦截）| ✅ 已实现 |
| 合规判断 | 触发规则说明 + 操作建议 | ✅ 已实现 |
| 报销记录 | 历史记录列表展示 | ✅ 已实现 |
| 帮助反馈 | 常见问题 / 联系方式 | ✅ 已实现 |

### 5.2 明确不在本版本范围内

| 功能 | 规划版本 | 说明 |
|------|----------|------|
| 真实 OCR 服务接入 | V1.1 | 当前为 mock 模拟 |
| 真实 LLM API 接入 | V1.1 | 当前为 mock 预设文本 |
| 数据库持久化存储 | V1.1 | 当前数据存于内存 |
| 登录鉴权 / 角色权限 | V1.1 | 当前无鉴权 |
| 批量发票处理 | V2.0 | 多张发票一次上传 |
| 管理员规则配置 | V2.0 | 自定义规则库 |
| 费控数据看板 | V3.0 | 驳回率趋势、费用分布 |

---

## 六、技术架构

### 6.1 系统架构（MVP）

```
前端（index.html）
    ↓ HTTP / JSON
后端（server.js · Node.js + Express）
    ├── POST /api/ocr          → OCR 识别（mock）
    ├── POST /api/precheck     → 合规规则判断（mock）
    └── GET  /api/reimbursements → 报销记录查询
```

### 6.2 AI 链路总览

| 节点 | 技术方案 | 当前实现 | 生产方案 |
|------|----------|----------|----------|
| 发票识别 | 多模态 OCR | Mock 数据 | GPT-4o Vision / 百度 OCR |
| 规则检索 | RAG 向量检索 | 硬编码规则匹配 | Embedding + 向量数据库 |
| 合规判断 | LLM + Prompt | Mock 预设输出 | Claude / GPT-4 API |
| 回答生成 | LLM 文本生成 | Mock 预设文本 | 同上，Prompt 控制输出格式 |

### 6.3 关键字段定义

| 字段名 | 类型 | 说明 | 必填 |
|--------|------|------|------|
| invoice_type | String | 发票类型（普票/专票/电子票）| 是 |
| invoice_number | String | 发票号码 | 否 |
| invoice_date | Date | 开票日期（YYYY-MM-DD）| 是 |
| merchant_name | String | 开票单位名称 | 是 |
| amount | Decimal | 不含税金额（元）| 是 |
| tax_amount | Decimal | 税额（元）| 否 |
| total_amount | Decimal | 价税合计（元）| 是 |
| expense_type | String | 费用类型（餐饮/住宿/交通等）| 是 |
| status | Enum | 合规状态（pass/warn/block/pending）| 是 |
| triggered_rule | String | 触发规则 ID | 否 |
| reason | String | 判断原因说明 | 是 |
| suggestion | String | 操作建议 | 是 |

---

## 七、数据埋点

### 7.1 核心行为事件

| 事件名 | 触发时机 | 关键参数 |
|--------|----------|----------|
| invoice_upload | 用户上传发票 | 上传方式、文件大小 |
| ocr_complete | OCR 识别完成 | 识别耗时、置信度均值 |
| field_edited | 用户手动修改字段 | 修改字段名、修改前后值 |
| precheck_complete | 合规判断完成 | 判断结果、触发规则、处理耗时 |
| suggestion_adopted | 用户采纳建议 | 采纳状态、修改内容 |

### 7.2 核心业务指标

| 指标 | 计算方式 | 目标值 |
|------|----------|--------|
| AI 判断采纳率 | 采纳数 / 总判断数 | ≥ 80% |
| 合规判断准确率 | 与财务最终判断一致 / 总数 | ≥ 95% |
| 平均响应时长 | OCR + 判断总耗时均值 | ≤ 7 秒 |
| 驳回率降低幅度 | （基线 - 当前）/ 基线 | ≥ 60% |

---

## 八、性能要求

| 功能 | 目标值 |
|------|--------|
| 页面首屏加载 | ≤ 2 秒 |
| 发票上传完成 | ≤ 1 秒 |
| OCR 识别返回 | ≤ 5 秒 |
| 合规判断返回 | ≤ 2 秒 |
| 端到端总响应 | ≤ 10 秒 |

---

## 九、风险与应对

| 风险 | 概率 | 应对措施 |
|------|------|----------|
| LLM 误判导致合规漏洞 | 中 | 关键规则设为"预警"而非直接通过，引导人工确认；界面明确标注"仅供参考，以财务审核为准" |
| OCR 识别准确率不足 | 中 | 低置信度字段强制人工确认，不自动填充 |
| 规则库覆盖不完整 | 中 | 未覆盖场景返回"待人工审核"，建立规则迭代机制 |
| 员工过度依赖 AI | 低 | 界面常驻提示"AI 预检仅供参考" |
| 发票数据隐私泄露 | 低 | 不持久化存储发票原始图片，数据加密传输 |

---

## 十、后续迭代规划

### V1.1（工程化）
- 接入真实 OCR 服务（百度 OCR / GPT-4o Vision）
- 接入真实 LLM API（Claude / GPT-4），替换 mock 输出
- 接入数据库，持久化用户、单据、规则、操作日志
- 接入登录鉴权与员工/财务/管理员角色权限

### V2.0（功能增强）
- 管理员规则配置界面（自定义规则库）
- 批量发票预检（多张一次上传）
- 合规判断可解释性增强（命中规则原文展示）

### V3.0（管理视角）
- 费控数据看板（驳回率趋势、预警来源分布、节省成本估算）
- 策略模拟（调整报销标准后的成本影响预测）

---

## 附录

### A. 术语表

| 术语 | 定义 |
|------|------|
| OCR | Optical Character Recognition，光学字符识别，将图片中的文字转为可处理的文本数据 |
| RAG | Retrieval-Augmented Generation，检索增强生成，先从知识库检索相关内容，再交给 LLM 生成回答 |
| LLM | Large Language Model，大语言模型 |
| Prompt | 输入给 LLM 的指令文本，决定模型的角色、任务、输出格式和约束条件 |
| 硬性违规（Block）| 触发不可逾越的规则，系统拦截，禁止提交 |
| 软性违规（Warn）| 触发需关注的规则，系统预警，填写说明后可提交 |
| MVP | Minimum Viable Product，最小可行产品 |

### B. 参考资料

- 项目仓库：https://github.com/wangaki973/precheck-reimburse-ai-mvp
- 竞品参考：合思（ekuaibao）、Zoho Expense、金蝶费控云

---

*文档结束 · AI 报销预检助手 PRD v1.3*
